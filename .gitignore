.build/
.buildServer.json

# Antigravity agent customizations
.agents/
.agent/

# Private agent instructions — kept out of version control
AGENTS.md
.claude/rules/

# MCP config contains a GitHub PAT token — do not commit
.mcp.json
# XcodeBuildMCP local tool config
.xcodebuildmcp/
